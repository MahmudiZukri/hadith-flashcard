part of 'pages.dart';

class Wrapper extends StatelessWidget {
  const Wrapper({
    super.key,
    required this.user,
  });

  final auth.User user;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
